/cnc_machine_old.py
/__pycache__
__pycache__/
*.pyc
*.egg-info/
.venv/
*.venv

# Python build artifacts
build/
dist/
*.egg
.pytest_cache/

# Per-protocol runtime output (calibration files, deck state snapshots)
examples/**/output/

# BOM master spreadsheets (CSV exports are the tracked source of truth)
*.xlsx

# Large demo videos — hosted on YouTube, only the preview gifs are tracked
media/*.mp4
media/*.mov
media/*.webm

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local secrets (PyPI token, etc.) \u2014 never commit
.pypirc
.secrets/

